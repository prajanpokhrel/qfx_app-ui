import 'package:flutter/material.dart';

class MyloyaltyScreen extends StatelessWidget {
  const MyloyaltyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color.fromARGB(255, 63, 61, 61),
        toolbarHeight: 70,
      ),
    );
  }
}
